service: kjglass-mailer

provider:
  name: aws
  runtime: nodejs14.x
  region: ap-northeast-2

functions:
  email:
    handler: index.handler
    events:
      - http:
          path: send-email
          method: POST
          cors: true
